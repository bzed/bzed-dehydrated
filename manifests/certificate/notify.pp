# @summary class is notified on all certificate changes
#
# This class is being notified from all certificate related changes.
# Subscribe on it to have your services restarted or to make other
# things happen automatically.
#
# @example
#   include dehydrated::certificate::notify
class dehydrated::certificate::notify {
}
